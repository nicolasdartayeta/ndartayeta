---
import { getEntry } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import Experience from '../components/cv/Experience.astro';
import Education from '../components/cv/Education.astro';
import Skills from '../components/cv/Skills.astro';
import { SITE_TITLE } from '../consts';

const entry = await getEntry('cv', 'main');
const { experience, education, skills } = entry!.data;
---

<Layout title={`CV — ${SITE_TITLE}`} description="Work experience, education, and skills.">

  <div class="mb-12">
    <h1 class="mb-2">Curriculum Vitae</h1>
    <p class="text-muted text-base m-0">Software engineer with 7+ years building web products.</p>
  </div>

  <Experience items={experience} />
  <Education items={education} />
  <Skills skills={skills} />

</Layout>
